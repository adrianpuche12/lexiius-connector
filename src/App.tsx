// La UI principal del Connector es solo el system tray (main.rs).
// Este componente existe como placeholder requerido por Tauri.
// No hay ventana visible — la app vive en el ícono del system tray.

export default function App() {
  return null;
}
